#Question 1:

import numpy as np

class My_Array:
    def __init__(self):
        self.size = 0
        self.capacity = 1
        self.array = np.zeros(self.capacity)

    def append(self, number : float):

        if self.capacity == self.size: #Verifica se a capacidade foi atingida. Caso isso tenha acontecido, vamos dobrar a capacidade e criar um novo array.
            self.capacity = 2*self.capacity
            new_list = np.zeros(self.capacity)
            for i in range(self.array.size):
                new_list[i] += self.array[i]
            self.array = new_list #Atualizamos o array
        if self.size < self.capacity: #Enquanto a capacidade não estiver lotada, vamos adicionando elementos
            self.array[self.size] = number
            self.size += 1
        return self.array

    def __str__(self): #Aqui criamos um método para mostrar o array
        return 'The array is {}'.format(self.array)

a = My_Array()

''''
Exemplos, caso queira:

a.append(2)    
print(a)        
a.append(3)
print(a)
a.append(4)
a.append(5)
print(a)
'''''


#Question 2:

class ToroArray:
    def __init__(self, arr):  #Vamos construir um array
        self.array = np.array(arr)

    def __getitem__(self, index):  #O índice do elemento que queremos corresponde ao resto da divisão do que chamamos de index pelo tamanaho do array.
        return self.array[index % len(self.array)]

tr = ToroArray([10, 11, 12, 13, 14])

'''''
Exemplo, caso queira:

print(tr[6])
print(tr[-20])   
'''''
